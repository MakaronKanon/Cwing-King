#ifndef WINDOWS_DELAY_H
#define WINDOWS_DELAY_H

/**
 * @brief blocks execution for some milliseconds.
 * @param milliSecs, how long to delay.
 */
void delayMilliSecs(unsigned int milliSecs);

#endif //WINDOWS_DELAY_H